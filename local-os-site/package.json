{
  "name": "local-os-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev --host 127.0.0.1 --port 4322",
    "build": "astro build",
    "export-feed": "node scripts/export-public-feed.mjs",
    "preview": "astro preview --host 127.0.0.1 --port 4322",
    "deploy": "node scripts/export-public-feed.mjs && astro build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.6.0",
    "astro": "^6.4.7"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
