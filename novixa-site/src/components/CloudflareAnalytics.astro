---
const token = import.meta.env.PUBLIC_CF_WEB_ANALYTICS_TOKEN?.trim();
const beacon = token ? JSON.stringify({ token }) : null;
---

{beacon && (
  <script
    defer
    src="https://static.cloudflareinsights.com/beacon.min.js"
    data-cf-beacon={beacon}
  />
)}
